#include <stdio.h>
/**
  *mul- multiplies two integers
  *@a:recieved an integer
  *@b: recieves an integer
  *Return: multi value
  */
int mul(int a, int b)
{
	int mult;

	mult = a * b;
	return (mult);
}
